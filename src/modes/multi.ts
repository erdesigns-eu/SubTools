import { askQuestion } from "../lib/common.js";

/**
 * Multi file mode.
 * In this mode, the user can provide a directory to process all SRT and Video files in.
 * @returns {Promise<void>} Nothing
 */
export default async function(): Promise<void> {
    
}